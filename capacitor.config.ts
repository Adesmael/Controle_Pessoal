import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'controlePessoal.com.app',
  appName: 'ControlePessoal',
  webDir: 'public'
};

export default config;
